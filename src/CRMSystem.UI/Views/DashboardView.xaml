<UserControl x:Class="CRMSystem.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:CRMSystem.UI.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Dashboard"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20" />

        <UniformGrid Grid.Row="1" Rows="1" Columns="3" Margin="0,0,0,20">
            <Border Background="#F0F4F8" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Total Clients" FontSize="14" Foreground="Gray" />
                    <TextBlock Text="{Binding TotalClients}" FontSize="32" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Background="#F0F4F8" CornerRadius="8" Padding="20" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="Total Contacts" FontSize="14" Foreground="Gray" />
                    <TextBlock Text="{Binding TotalContacts}" FontSize="32" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Background="#F0F4F8" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Contacts (Last 30 Days)" FontSize="14" Foreground="Gray" />
                    <TextBlock Text="{Binding ContactsLast30Days}" FontSize="32" FontWeight="Bold" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Foreground="Gray"
                   FontStyle="Italic" />
    </Grid>
</UserControl>